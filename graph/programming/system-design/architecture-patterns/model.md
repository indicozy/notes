Basically there is two type of Model:

- Data model — which is just simple dto
- Domain model (**not class!**) — your business logic, its concepts and rules

In all of those MV* patterns, M stands for **Domain model**.